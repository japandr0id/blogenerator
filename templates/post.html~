{% extends "base.html" %}

{% block content %}
<div>
  <h1 id="header">{{ post.title }}</h1>
  {% if post.subtitle %}
      <h2>{{ post.subtitle }}</h2>
  {% endif %}
  <h3 id="date">{{ post.date }}</h3>
</div>
<div id="content">
  {{ post.html|safe }}
</div>
{% endblock content %}
